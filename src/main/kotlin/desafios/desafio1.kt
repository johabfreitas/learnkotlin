package desafios

/**
 *  Crie um programa que imprima o tamanho (em bits) e os valores mínimo e
 *  máximo do Int tipo. Emita cada valor em uma nova linha.
 *  Aqui está um exemplo de saída com o Byte tipo:
 *   8
 *   -128
 *   127
 */

fun main() {
    println(Int.SIZE_BITS)
    println(Int.MIN_VALUE)
    println(Int.MAX_VALUE)
}